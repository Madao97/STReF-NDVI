# Core
numpy>=1.24
scipy>=1.10
pandas>=2.0
tqdm>=4.66
matplotlib>=3.8
scikit-image>=0.22
scikit-learn>=1.3
numba>=0.58

# Geospatial
rasterio>=1.3.9
geopandas>=0.14
shapely>=2.0
fiona>=1.9
pyproj>=3.6
affine>=2.4.0

# Optional: GPU array acceleration for older scripts (<2022)
# CuPy installation depends on your CUDA version. Install the appropriate package if needed, or skip it.
# cupy-cuda12x>=13.0 ; platform_system != "Darwin"
# cupy-cuda11x>=12.0 ; platform_system != "Darwin"

# ML / DL
# We strongly recommend installing PyTorch following the official instructions (see README).
# torch>=2.1
# torchvision>=0.16
# PyTorch Geometric should also be installed following the official installation guide (see README).
# torch-geometric>=2.5

# GDAL
# We recommend installing GDAL via conda-forge (see README).
# If using pip, make sure the GDAL version is compatible with the GDAL libraries installed on your system.
# GDAL>=3.4
